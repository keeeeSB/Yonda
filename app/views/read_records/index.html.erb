<% provide(:title, "ホーム") %>
<h1 class="text-center p-5">みんなの記録</h1>

<div class="container">
  <% @read_records.each do |read_record| %>
    <div class="card mb-3 mx-auto" style="max-width: 540px;">
      <div class="row g-0">
        <div class="col-md-4">
          <%= image_tag read_record.book.image_link, class: "img-fluid rounded-start" %>
        </div>
        <div class="col-md-8">
          <div class="card-body">
            <h5 class="card-title"><%= read_record.book.title %></h5>
            <p class="card-text"><%= read_record.read_date %></p>
            <h5>よんだ人 : <%= read_record.user.name %></h4>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
