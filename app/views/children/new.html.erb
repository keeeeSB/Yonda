<% provide(:title, "お子さま登録") %>
<h1 class="text-center p-5">お子さま登録</h1>

<div class="row">
  <div class="col-md-10 col-lg-8 mx-auto w-50">
  <%= form_with(model: @child) do |f| %>
    <div class="mb-3">
      <%= f.label :name, class: "form-label" %>
      <%= f.text_field :name, class: "form-control" %>
    </div>
    <div class="mb-3">
      <%= f.label :gender, class: "form-label" %>
      <%= f.select :gender, ["男の子", "女の子"], class: "form-control" %>
    </div>
    <div class="mb-3">
      <%= f.label :birthday, class: "form-label" %>
      <%= f.date_select :birthday, { prompt: { day: '日', month: '月', year: '年' }, start_year: Date.current.year - 6, end_year: Date.current.year, order: [:year, :month, :day] }, class: "form-control" %>
    </div>
    <div class="mb-3">
      <%= f.label :family_id, class: "form-label" %>
      <%= f.select :family_id, options_for_select(Family.all.map { |family| [family.name, family.id] }), { include_blank: true }, class: "form-control" %>
    </div>
    <div class="mb-3 d-grid gap-2">
      <%= f.submit "登録", class: "btn btn-warning btn-lg" %>
    </div>
  <% end %>
  </div>
</div>
